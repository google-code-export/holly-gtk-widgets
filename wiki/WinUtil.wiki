#summary windows util

|| _ Gtk.Window _ || * getTopWindow * || ( Gtk.Widget widget ) || returns the widget's parent window ||
|| _ bool       _ || * ModifyWindowShape * || ( System.Drawing.Bitmap bmp, Gtk.Window window ) || modifies the window shape according to the passed bitmap. It supports transparency. ||